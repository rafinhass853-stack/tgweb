rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite tudo temporariamente para teste
    match /{document=**} {
      allow read, write: if true;
    }
  }
}